# Disable bento's default oh-my-zsh setup.
# We manage oh-my-zsh via ~/.zshrc.common instead.
# if [[ ! -f "$HOME/.zshrc.d/oh-my-zsh.zsh" ]]; then
#     source "$HOME/.oh-my-zsh/templates/zshrc.zsh-template"
# fi
